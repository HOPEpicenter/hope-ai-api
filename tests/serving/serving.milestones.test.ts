import { buildServingMilestones } from "../../src/domain/serving/serving.milestones";
describe("Serving milestones", () => { it("counts first activities and lifecycle milestones", () => { const milestones = buildServingMilestones([{ occurredAt: "2026-09-01T00:00:00.000Z", type: "ServingAssignmentStarted", assignmentId: "assignment-1" }, { occurredAt: "2026-09-02T00:00:00.000Z", type: "ServingActivityRecorded", assignmentId: "assignment-1" }]); expect(milestones).toMatchObject({ totalAssignmentsStarted: 1, totalActivitiesRecorded: 1 }); expect(milestones.firstActivitiesRecorded).toHaveLength(1); }); });