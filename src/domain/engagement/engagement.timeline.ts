import type { EngagementEvent } from "./engagement.events";
import type { EngagementProfile } from "./engagementProfile.projection";
export type EngagementTimelineItem = { occurredAt: string; type: EngagementEvent["type"]; engagementCycleId: string; touchpointId?: string; channel?: string; scoreDelta?: number; notes?: string; reason?: string; statusChange: "started" | "touchpoint_added" | "stalled" | "completed" };
export type EngagementTimelineCollection = { memberId: string; items: EngagementTimelineItem[] };
export function buildEngagementTimeline(profile: EngagementProfile, events: readonly EngagementEvent[]): EngagementTimelineItem[] { return events.filter((event) => event.memberId === profile.memberId).slice().sort((left, right) => left.occurredAt.localeCompare(right.occurredAt) || left.eventId.localeCompare(right.eventId)).map((event) => { switch (event.type) { case "EngagementCycleStarted": return { occurredAt: event.occurredAt, type: event.type, engagementCycleId: event.engagementCycleId, statusChange: "started" }; case "EngagementTouchpointAdded": return { occurredAt: event.occurredAt, type: event.type, engagementCycleId: event.engagementCycleId, touchpointId: event.payload.touchpointId, channel: event.payload.channel, scoreDelta: event.payload.scoreDelta, notes: event.payload.notes, statusChange: "touchpoint_added" }; case "EngagementStalledDetected": return { occurredAt: event.occurredAt, type: event.type, engagementCycleId: event.engagementCycleId, reason: event.payload.reason, statusChange: "stalled" }; case "EngagementCycleCompleted": return { occurredAt: event.occurredAt, type: event.type, engagementCycleId: event.engagementCycleId, statusChange: "completed" }; } }); }