import type { CommunityInsights } from "./community.insights";
import type { CommunityProfile } from "./communityProfile.projection";
export type CommunityRecommendation = { memberId: string; action: "address_stalled_engagement" | "suggest_group" | "encourage_interaction" | "continue_community" | "no_recommendation"; engagementId: string | null; reason: string };
export function buildCommunityRecommendation(profile: CommunityProfile, insights: CommunityInsights): CommunityRecommendation { const stalled = profile.history.find((engagement) => engagement.status === "stalled"); if (stalled) return { memberId: profile.memberId, action: "address_stalled_engagement", engagementId: stalled.engagementId, reason: "A community engagement is stalled and needs follow-up." }; const unassigned = profile.history.find((engagement) => engagement.status !== "completed" && !engagement.groupId); if (unassigned) return { memberId: profile.memberId, action: "suggest_group", engagementId: unassigned.engagementId, reason: "An active community engagement has no group." }; const inactive = profile.history.find((engagement) => engagement.status === "active" && engagement.interactions.length === 0); if (inactive) return { memberId: profile.memberId, action: "encourage_interaction", engagementId: inactive.engagementId, reason: "An active community engagement has no recorded interaction." }; const active = profile.history.find((engagement) => engagement.status === "active"); if (active) return { memberId: profile.memberId, action: "continue_community", engagementId: active.engagementId, reason: "An active community engagement should receive its next check-in." }; return { memberId: profile.memberId, action: "no_recommendation", engagementId: null, reason: insights.completedEngagementCount ? "All community engagements are completed." : "No community engagements are available." }; }