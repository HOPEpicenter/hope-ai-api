import { Router } from "express";
import { AttendanceController } from "./attendance.controller";
export function createAttendanceRoutes(controller: AttendanceController): Router { const router = Router(); router.get("/attendance/profile/:memberId", (req, res) => res.json(controller.getProfile(req.params.memberId))); router.get("/attendance/timeline/:memberId", (req, res) => res.json(controller.getTimeline(req.params.memberId))); router.get("/attendance/milestones/:memberId", (req, res) => res.json(controller.getMilestones(req.params.memberId))); router.get("/attendance/insights/:memberId", (req, res) => res.json(controller.getInsights(req.params.memberId))); router.get("/attendance/recommendation/:memberId", (req, res) => res.json(controller.getRecommendation(req.params.memberId))); router.get("/attendance/coaching/:memberId", (req, res) => res.json(controller.getCoaching(req.params.memberId))); router.get("/attendance/report/:memberId", (req, res) => res.json(controller.getReport(req.params.memberId))); return router; }