export const TIMELINE_DERIVATION_LIMIT = 20; // used for summaries, journey, dashboard
export const TIMELINE_PREVIEW_LIMIT = 20;    // UI preview surfaces (keep same for now)
export const OPS_TIMELINE_LIMIT = 100;       // ops/debug deeper inspection
