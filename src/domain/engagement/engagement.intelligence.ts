import type { EngagementInsights } from "./engagement.insights";
import type { EngagementProfile } from "./engagementProfile.projection";
export type EngagementRecommendation = { memberId: string; action: "address_stalled_cycle" | "reengage_member" | "continue_engagement" | "no_recommendation"; engagementCycleId: string | null; reason: string };
export function buildEngagementRecommendation(profile: EngagementProfile, insights: EngagementInsights): EngagementRecommendation { const stalled = profile.cycles.find((cycle) => cycle.stalledSince !== null); if (stalled) return { memberId: profile.memberId, action: "address_stalled_cycle", engagementCycleId: stalled.engagementCycleId, reason: "An engagement cycle is stalled and needs follow-up." }; if (insights.engagementTrend === "declining" || insights.engagementScore < 0) return { memberId: profile.memberId, action: "reengage_member", engagementCycleId: profile.cycles.at(-1)?.engagementCycleId ?? null, reason: "Engagement signals are declining." }; if (profile.touchpointCount) return { memberId: profile.memberId, action: "continue_engagement", engagementCycleId: profile.cycles.at(-1)?.engagementCycleId ?? null, reason: "Continue the current engagement rhythm." }; return { memberId: profile.memberId, action: "no_recommendation", engagementCycleId: null, reason: "No engagement activity is available." }; }