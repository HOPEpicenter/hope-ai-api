import type { AttendanceInsights } from "./attendance.insights";
import type { AttendanceProfile } from "./attendanceProfile.projection";
export type AttendanceRecommendation = { memberId: string; action: "address_stalled_attendance" | "follow_up_absence" | "encourage_consistency" | "continue_attendance" | "no_recommendation"; attendanceId: string | null; reason: string };
export function buildAttendanceRecommendation(profile: AttendanceProfile, insights: AttendanceInsights): AttendanceRecommendation { const stalled = profile.records.find((record) => record.stalledSince !== null); if (stalled) return { memberId: profile.memberId, action: "address_stalled_attendance", attendanceId: stalled.attendanceId, reason: "An attendance record is stalled and needs follow-up." }; const absent = profile.records.filter((record) => record.status === "absent").at(-1); if (absent && insights.attendanceRate < 0.75) return { memberId: profile.memberId, action: "follow_up_absence", attendanceId: absent.attendanceId, reason: "Recent attendance is below the consistency threshold." }; if (profile.records.length) return { memberId: profile.memberId, action: "continue_attendance", attendanceId: profile.records.at(-1)?.attendanceId ?? null, reason: "Continue recognizing consistent attendance." }; return { memberId: profile.memberId, action: "no_recommendation", attendanceId: null, reason: "No attendance records are available." }; }