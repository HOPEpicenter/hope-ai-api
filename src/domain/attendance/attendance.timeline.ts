import type { AttendanceEvent } from "./attendance.events";
import type { AttendanceProfile } from "./attendanceProfile.projection";
export type AttendanceTimelineItem = { occurredAt: string; type: AttendanceEvent["type"]; attendanceId: string; status?: "present" | "absent" | "late"; notes?: string; reason?: string; statusChange?: "recorded" | "updated" | "stalled" | "completed" };
export type AttendanceTimelineCollection = { memberId: string; items: AttendanceTimelineItem[] };
export function buildAttendanceTimeline(profile: AttendanceProfile, events: readonly AttendanceEvent[]): AttendanceTimelineItem[] { return events.filter((event) => event.memberId === profile.memberId).slice().sort((left, right) => left.occurredAt.localeCompare(right.occurredAt) || left.eventId.localeCompare(right.eventId)).map((event) => { switch (event.type) { case "AttendanceRecorded": return { occurredAt: event.occurredAt, type: event.type, attendanceId: event.attendanceId, status: event.payload.status, notes: event.payload.notes, statusChange: "recorded" }; case "AttendanceStatusUpdated": return { occurredAt: event.occurredAt, type: event.type, attendanceId: event.attendanceId, status: event.payload.status, notes: event.payload.notes, statusChange: "updated" }; case "AttendanceStalledDetected": return { occurredAt: event.occurredAt, type: event.type, attendanceId: event.attendanceId, reason: event.payload.reason, statusChange: "stalled" }; case "AttendanceCycleCompleted": return { occurredAt: event.occurredAt, type: event.type, attendanceId: event.attendanceId, statusChange: "completed" }; } }); }