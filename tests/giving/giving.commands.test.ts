import { handleRecordGift, handleUpdateGiftDesignation } from "../../src/domain/giving/giving.commands";
describe("Giving commands", () => { it("creates typed gift events and rejects invalid amounts", () => { expect(handleRecordGift({ memberId: "m", giftId: "g", amount: 25, designation: "general" }).payload).toEqual({ amount: 25, designation: "general" }); expect(handleUpdateGiftDesignation({ memberId: "m", giftId: "g", designation: "missions" }).type).toBe("GiftDesignationUpdated"); expect(() => handleRecordGift({ memberId: "m", giftId: "g", amount: 0, designation: "general" })).toThrow("amount"); }); });