import { Router } from "express";
import { GivingController } from "./giving.controller";
export function createGivingRoutes(controller: GivingController): Router { const router = Router(); router.get("/giving/profile/:memberId", (req, res) => res.json(controller.getProfile(req.params.memberId))); router.get("/giving/timeline/:memberId", (req, res) => res.json(controller.getTimeline(req.params.memberId))); router.get("/giving/milestones/:memberId", (req, res) => res.json(controller.getMilestones(req.params.memberId))); router.get("/giving/insights/:memberId", (req, res) => res.json(controller.getInsights(req.params.memberId))); router.get("/giving/recommendation/:memberId", (req, res) => res.json(controller.getRecommendation(req.params.memberId))); router.get("/giving/coaching/:memberId", (req, res) => res.json(controller.getCoaching(req.params.memberId))); router.get("/giving/report/:memberId", (req, res) => res.json(controller.getReport(req.params.memberId))); return router; }