import type { MinistryHealthTimelineItem } from "./ministryHealth.timeline";
export type MinistryHealthMilestones = { healthyDomains: number; watchDomains: number; attentionDomains: number; alertsRaised: number; firstAlert: MinistryHealthTimelineItem | null };
export function buildMinistryHealthMilestones(timeline: readonly MinistryHealthTimelineItem[]): MinistryHealthMilestones { const scores = timeline.filter(item => item.type === "score_observed"); const alerts = timeline.filter(item => item.type === "alert_raised"); return { healthyDomains: scores.filter(item => (item.score ?? 0) >= 70).length, watchDomains: scores.filter(item => (item.score ?? 0) >= 40 && (item.score ?? 0) < 70).length, attentionDomains: scores.filter(item => (item.score ?? 0) < 40).length, alertsRaised: alerts.length, firstAlert: alerts[0] ?? null }; }