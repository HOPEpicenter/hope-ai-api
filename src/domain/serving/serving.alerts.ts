import type { ServingInsights } from "./serving.insights";
import type { ServingProfile } from "./servingProfile.projection";
export type ServingAlert = { memberId: string; assignmentId: string; signalType: "ServingAssignmentStalled" | "HighPriorityServingAssignment" | "UnassignedServingRole"; severity: "medium" | "high"; message: string };
export function buildServingAlerts(profile: ServingProfile, insights: ServingInsights): ServingAlert[] { const alerts: ServingAlert[] = []; for (const assignment of profile.history) { if (assignment.status === "stalled") alerts.push({ memberId: profile.memberId, assignmentId: assignment.assignmentId, signalType: "ServingAssignmentStalled", severity: "high", message: "Serving assignment is stalled." }); if (assignment.status !== "closed" && assignment.priority === "high") alerts.push({ memberId: profile.memberId, assignmentId: assignment.assignmentId, signalType: "HighPriorityServingAssignment", severity: "high", message: "High-priority serving assignment is active." }); if (assignment.status !== "closed" && !assignment.roleId) alerts.push({ memberId: profile.memberId, assignmentId: assignment.assignmentId, signalType: "UnassignedServingRole", severity: "medium", message: "Active serving assignment has no role." }); } return insights.needsAttention ? alerts : []; }