import type { ServingEvent } from "./serving.events";
import type { ServingProfile } from "./servingProfile.projection";
export type ServingTimelineItem = { occurredAt: string; type: ServingEvent["type"]; assignmentId: string; roleId?: string | null; priority?: string; activity?: string; reason?: string; statusChange?: "active" | "stalled" | "closed" };
export type ServingTimelineCollection = { memberId: string; items: ServingTimelineItem[] };
export function buildServingTimeline(profile: ServingProfile, events: readonly ServingEvent[]): ServingTimelineItem[] { return events.filter((event) => event.memberId === profile.memberId).slice().sort((left, right) => left.occurredAt.localeCompare(right.occurredAt) || left.eventId.localeCompare(right.eventId)).map((event) => { switch (event.type) { case "ServingAssignmentStarted": return { occurredAt: event.occurredAt, type: event.type, assignmentId: event.assignmentId, roleId: event.payload.roleId, priority: event.payload.priority, statusChange: "active" }; case "ServingRoleAssigned": return { occurredAt: event.occurredAt, type: event.type, assignmentId: event.assignmentId, roleId: event.payload.roleId }; case "ServingActivityRecorded": return { occurredAt: event.occurredAt, type: event.type, assignmentId: event.assignmentId, activity: event.payload.activity, statusChange: "active" }; case "ServingStalledDetected": return { occurredAt: event.occurredAt, type: event.type, assignmentId: event.assignmentId, reason: event.payload.reason, statusChange: "stalled" }; case "ServingAssignmentClosed": return { occurredAt: event.occurredAt, type: event.type, assignmentId: event.assignmentId, statusChange: "closed" }; } }); }